function sum (a, b) {
  console.log('The sum of ' + a + ' and ' + b + ' is ' + (a + b));
  return(a + b);
}

function multiply (a, b) {
  console.log('The product of ' + a + ' and ' + b + ' is ' + (a * b));
  return(a * b);
}
